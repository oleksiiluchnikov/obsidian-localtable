/** Canonical plugin identifier used for manifest, storage, and protocol names. */
export const PLUGIN_ID = "localtable";

/** Canonical plugin name used in user-facing labels. */
export const PLUGIN_NAME = "localtable";

/** View type for the main linked-record browser pane. */
export const VIEW_TYPE_AIRTABLE = `${PLUGIN_ID}-view`;

/** Cache persistence filename inside the plugin data directory. */
export const CACHE_STORAGE_KEY = PLUGIN_ID;
